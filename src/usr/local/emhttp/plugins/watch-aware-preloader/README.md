**Watch-Aware Preloader**

Warms the Linux page cache with the media each household user is most likely to
play next, so playback starts instantly instead of waiting for an array disk to
spin up. Preload decisions come from the media server's own watch state
(resume points, next-up episodes, recently added) rather than filesystem mtime.
